1.View the history of the repository
git log

2.If there are outstanding commits to do
git commit -m "Commit message"

3.Start a new repository
git init
and
git add (to add files to staging area before commit to the new repo)
